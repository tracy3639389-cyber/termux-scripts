# termux-scripts
build中转服务器代码
# Tracy Server - 个人中转服务

这是专为 Termux 设计的轻量级中转服务器。

## 🚀 如何安装

1. 下载并安装 **Termux** App。
2. 打开 Termux，复制下面这行命令，粘贴并回车：

```bash
pkg update -y && pkg upgrade -y && pkg install curl -y && bash <(curl -L https://cdn.jsdelivr.net/gh/tracy3639389-cyber/termux-scripts@main/install.sh)

